# Discrepancies v2

No material discrepancy was found.

One minor, non-failing denominator mismatch remains: `run/reconstruction.json` reports 15/15 in its self-authored `coreEvidence` matrix and enumerates the probe's 8 critical questions, while the independent audit defines 16 core units and 18 critical questions. Direct evaluation finds all external items covered, so the canonical counts remain 16/16 and 18/18.
