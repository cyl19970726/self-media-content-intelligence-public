# Schema validation

结果：`PASS`。

使用 canonical `validate-schemas.py` 验证了 `probe`、`protocol`、`reconstruction`、`evaluation` 与 `ocr`；没有 schema failure。

Schema 通过不等于 reconstruction ready；deterministic/independent hard gates 仍有 2 项失败。
