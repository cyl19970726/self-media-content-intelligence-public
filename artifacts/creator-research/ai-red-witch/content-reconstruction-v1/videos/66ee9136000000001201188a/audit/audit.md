# 独立事实审计：66ee9136000000001201188a

## 结论

**部分有画面支持，但核心效率主张与完整工作流均未被证明。** 原片确实展示了一个已载入的 10 秒实拍片段、一段英文提示词、`In queue` 状态、标为 `Gen-3 Alpha` 的结果区，以及一个 10 秒 CG 动画结果。不过，这不是连续的端到端操作录屏：没有选择文件、真实上传、输入提示词、点击生成、连续等待、完成事件，也没有任何导出或下载动作。

画面标题“**1分钟做科幻大片**”没有计时证据。相反，约 9.25 秒的队列面板明确写着：`Your video is in queue and will start in a few minutes.`（视频正在排队，将在几分钟后开始）。因此，“一分钟完成”最多是营销标题，不能当作此视频已证明的事实。

## 媒体事实

- 容器时长：18.136190 秒；视频流时长：18.133333 秒。
- 画面：720 × 1280，30 fps，H.264。
- 音频：有，AAC。
- SRT：4 段，覆盖 0.000–18.134 秒；与 evidence pack 中的字幕一致。
- evidence pack：26 张 0.75 秒间隔的密集帧；包内只检出 2.1 秒一个场景切点，未捕捉后半段屏幕内容的多次剪辑变化，因此本审计也直接核对了原 MP4 的相应时刻。

## 18 秒演示实际展示了什么

| 原片时间 | 直接可见内容 | 能证明什么 | 不能证明什么 |
|---|---|---|---|
| 0.0–9.1 秒 | 多组“AI生成 / 实拍”上下分屏的科幻、动作画面 | 视频使用了 AI 与实拍对照的宣传表达 | 素材来源、生成模型、参数、成本、生成过程、是否做过后期 |
| 约 9.2–10.9 秒 | 已载入实拍片段；右侧为 `In queue`；输入播放器显示总长 10 秒 | 界面中存在一段 10 秒输入和排队状态 | 选择文件、上传、上传耗时、队列耗时 |
| 约 11.0–13.4 秒 | 输入播放器与预先存在的英文提示词 | 提示词文本确实可见 | 输入提示词的动作、模型选择、点击生成 |
| 约 13.5–15.4 秒 | 画面切到输入与 CG 结果并排 | 有一个与输入人物动作大致对应的 CG 结果被展示 | 该结果是否在本次录制中生成、生成花了多久 |
| 约 15.5–18.136 秒 | 结果区特写；`Gen-3 Alpha`、10 秒播放器、`You're ready to generate.` | 结果区的模型标签与结果时长可读 | “刚刚完成”的状态、导出/下载、成品文件 |

## 输入、提示词、队列、模型和结果

### 输入

- 界面标题显示文件名：`9月21日(1).mp4`。
- 播放器总时长：`0:10`。
- 内容：一名红发女性穿灰色无袖上衣，在明亮室内说话、做手势。
- 状态：只证明“界面里已经有这个输入”；没有看到文件选择器、上传按钮、上传进度或完成提示。

### 提示词

界面可见的英文原文是：

> Sci-fi movie style, a female soldier sitting and giving a speech, cinematic texture, CG animation

黄色中文“科幻风格，女士坐着发表演讲，CG动画”是短视频后期叠字，不是产品输入框里的内容。视频没有展示键入过程。

### 队列

- 状态：`In queue`。
- 文案：`Your video is in queue and will start in a few minutes.`
- 没有显示进入队列的时间、队列进度、实际离队时刻或连续等待过程。

### 模型

- 结果区顶部可见：`Gen-3 Alpha`。
- 视频没有展示选择该模型的动作，也不能独立证明展示的结果确由这个模型在本次录制中生成。

### 结果

- 结果播放器总时长：`0:10`；约 17.25 秒时播放器显示 `0:02 / 0:10`。
- 内容：红发 CG/动画女性，穿深色金属质感服装，在蓝灰色室内做讲话手势。
- 与提示词的“科幻 / CG / 女性讲话者”大致相符；是否“坐着”无法从取景中可靠判断。
- 结果区可见 `Extend`、`Lip Sync` 操作，但没有证据表明使用过 Lip Sync，整体口播音轨也不能证明结果自身带音频。
- 顶部提示是 `You're ready to generate.`，不是一个明确绑定到该结果的“生成完成”事件。

## 生成链与导出链

### 生成链：只展示了头尾状态，过程被剪掉

已展示：

1. 已载入输入；
2. 已存在提示词；
3. `In queue`；
4. 剪辑后直接出现结果。

未展示：提示词输入、模型选择、其他生成设置、点击 Generate、连续等待、处理进度、错误或重试、完成通知，以及可核验的端到端用时。

### 导出链：完全未展示

整段 18.136 秒内没有看到打开下载/导出菜单、选择分辨率/格式/帧率/码率/音频、开始导出、导出进度、保存位置、导出文件或导出文件回放。因此不能从本视频推出“生成后可按某参数成功导出”的任何结论。

## 主张核查

| 主张 | 判定 | 依据与边界 |
|---|---|---|
| “1分钟做科幻大片” | **未证明，且与可见队列文案有张力** | 无端到端计时；界面反而写着“几分钟后开始”。18 秒成片时长不是工作流耗时。 |
| “上传一段实拍，再写一段提示词” | **部分支持** | 输入与提示词都可见，但上传和输入动作均未出现。 |
| “很快，一段视频就渲染好了” | **结果可见，速度未验证** | 结果通过剪辑直接出现，没有生成耗时。 |
| “操作非常简单 / 有手就行 / 小白有手就行” | **主观且未做端到端证明** | 关键设置、提交、等待、失败处理和导出均省略。 |
| “在家也能拍好莱坞大片” | **营销夸张，无法客观核验** | 只看到短 CG 示例，没有质量基准、成本、完整流程或素材来源。 |

## 仍然未知

- 展示的是实时 Runway 会话，还是预录/摆拍界面。
- 结果是否确由画面中的输入与提示词生成。
- 实际上传、排队、生成及总耗时；是否有失败、重试与额外人工处理。
- 账号套餐、积分成本、地区可用性。
- 分辨率、画幅、种子、运动控制、变体等生成参数。
- 输入与结果自身是否含可用音频，是否使用 Lip Sync。
- 导出格式、设置与导出文件完整性。
- 前半段多组“AI生成 / 实拍”素材的来源及对应关系。
- 是否做过第三方剪辑、合成、调色或其他后期。

## 证据定位

- 队列与已载入输入：原片约 9.25 秒；`evidence/frames/dense/dense-0014.jpg`、`dense-0015.jpg`。
- 10 秒输入与英文提示词：约 11.25–13.25 秒；`dense-0016.jpg`–`dense-0018.jpg`。
- 输入/结果并排：约 13.5–15.25 秒；`dense-0019.jpg`–`dense-0021.jpg`。
- `Gen-3 Alpha`、10 秒结果与 `You're ready to generate.`：约 15.75–18.0 秒；`dense-0022.jpg`–`dense-0025.jpg`。

审计仅使用指定原 MP4、SRT、新 evidence pack 及其 evidence 帧；未读取 Skill、candidate、旧报告或 skill-run 内容。
