# 差异清单：67124e580000000021000df9

本清单只记录 independent audit 与 skill-run candidate 之间的差异，不修改 candidate。

| ID | 严重度 | 差异 | 独立证据 | 对评估的影响 |
|---|---|---|---|---|
| D-01 | hard-gate | 候选漏掉 CUE-014 的字幕冲突：SRT 写“一段辅助 Vlog”，烧录字幕为“一段独居 Vlog”。候选保留了 SRT 原文，却没有像 CUE-004 那样建立冲突账本。 | audit `transcriptFindings` CUE-014；00:50.240–00:52.700 | 关键问题召回、证据覆盖、元闸失败。 |
| D-02 | hard-gate | 候选漏掉即梦 `16/16 生成完成` 状态。 | audit GT-03；生成段末尾 | UI 进度/完成状态缺失；也未显式守住“16/16 不推出提示词数、下载数或用图数”的关系。 |
| D-03 | major | 候选漏掉生成按钮消耗标记 `1`。 | audit GT-03 | 即梦可见参数/资源状态恢复不完整。仅能记为界面标记，不能外推实际价格或总成本。 |
| D-04 | major | 候选漏掉 Kimi “已阅读55个网页”的后段状态。 | audit GT-02 | 未恢复一项重要 UI 状态，也未讨论该状态不能替代逐项来源归属。 |
| D-05 | major | 候选漏掉剪辑界面可见音乐电平 `-1.8 dB`。 | audit GT-04 | 编辑工程的可见参数恢复不完整；该数值只属于所示工程状态。 |
| D-06 | hard-gate | 非语音音频只完成技术检查，没有完成语义听辨。候选证明了有信号、无长静音和样片响度，却不能判断实际是鸟叫、流水、音乐还是其他声音。 | skill-run `inspection/final-audio.mp3` 与 `audio-analysis.md` | 一个可用载体仍未检查；unchecked channels 必须为 0，因此硬闸失败。 |
| D-07 | meta-gate | 候选的自报 `metaGate.pass=true` 与独立元审计不一致。 | D-01、D-02、D-06 | 产物不能自证完备；元闸必须以独立遗漏搜索为准。 |

## 不构成错误的差异

- 候选没有把 `16/16` 写成“生成了 16 张并全部用于成片”；这是正确克制，但由于状态本身完全遗漏，仍算覆盖缺口。
- 候选把 2:36:03 解释为所示剪辑工程状态，而非 6.234 秒摘录或导出文件时长；与 audit 一致。
- 候选将产品称为“状物”，并把真实品牌/商业关系保留为未知；与 audit 一致。
- 候选将人物一致性写成方法主张未被结果严格证明；与 audit 一致。
- CUE-015 比媒体时长晚约 0.008 秒是源 SRT 的轻微缺陷，不构成候选时间定位错误。
